<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MatchMapper">

	<!-- 경기 정보 UPSERT (MERGE) -->
	<insert id="mergeMatch" parameterType="Matches">
		MERGE INTO MATCHES m
		USING (SELECT #{matchId} AS MATCH_ID FROM DUAL) s
		ON (m.MATCH_ID = s.MATCH_ID)
		WHEN MATCHED THEN
			UPDATE SET 
				m.MATCH_DATE = #{matchDate},
				m.HOME_SCORE = #{homeScore, jdbcType=NUMERIC},
				m.AWAY_SCORE = #{awayScore, jdbcType=NUMERIC},
				m.STATUS = #{status},
				m.ENDED_AT = #{endedAt, jdbcType=TIMESTAMP}
		WHEN NOT MATCHED THEN
			INSERT (MATCH_ID, MATCH_DATE, HOME_TEAM_ID, AWAY_TEAM_ID, HOME_SCORE, AWAY_SCORE, STATUS, NOTICE, ENDED_AT)
			VALUES (
				#{matchId},
				#{matchDate},
				#{homeTeamId},
				#{awayTeamId},
				#{homeScore, jdbcType=NUMERIC},
				#{awayScore, jdbcType=NUMERIC},
				#{status},
				#{notice, jdbcType=CLOB},
				#{endedAt, jdbcType=TIMESTAMP}
			)
	</insert>

	<!-- 전체 경기 목록 조회 (TEAMS 테이블 조인하여 HOME_GROUND_KOR, TEAM_NAME_KOR, EMBLEM_URL 함께 조회) -->
	<select id="selectAllMatches" resultType="Matches">
		SELECT 
			m.MATCH_ID,
			m.MATCH_DATE,
			m.HOME_TEAM_ID,
			m.AWAY_TEAM_ID,
			m.HOME_SCORE,
			m.AWAY_SCORE,
			m.STATUS,
			m.NOTICE,
			m.ENDED_AT,
			h.TEAM_NAME AS HOME_TEAM_NAME,
			h.TEAM_NAME_KOR AS HOME_TEAM_NAME_KOR,
			h.EMBLEM_URL AS HOME_EMBLEM_URL,
			h.HOME_GROUND,
			h.HOME_GROUND_KOR,
			a.TEAM_NAME AS AWAY_TEAM_NAME,
			a.TEAM_NAME_KOR AS AWAY_TEAM_NAME_KOR,
			a.EMBLEM_URL AS AWAY_EMBLEM_URL
		FROM MATCHES m
		LEFT JOIN TEAMS h ON h.TEAM_ID = m.HOME_TEAM_ID
		LEFT JOIN TEAMS a ON a.TEAM_ID = m.AWAY_TEAM_ID
		ORDER BY m.MATCH_DATE ASC
	</select>

	<!-- 특정 날짜 범위 경기 조회 -->
	<select id="selectMatchesByDateRange" parameterType="map" resultType="Matches">
		SELECT 
			MATCH_ID,
			MATCH_DATE,
			HOME_TEAM_ID,
			AWAY_TEAM_ID,
			HOME_SCORE,
			AWAY_SCORE,
			STATUS,
			NOTICE,
			ENDED_AT
		FROM MATCHES
		WHERE MATCH_DATE &gt;= #{startDate} AND MATCH_DATE &lt;= #{endDate}
		ORDER BY MATCH_DATE ASC
	</select>

	<!-- 단건 경기 상세 조회 -->
	<select id="selectMatchById" parameterType="long" resultType="Matches">
		SELECT 
			MATCH_ID,
			MATCH_DATE,
			HOME_TEAM_ID,
			AWAY_TEAM_ID,
			HOME_SCORE,
			AWAY_SCORE,
			STATUS,
			NOTICE,
			ENDED_AT
		FROM MATCHES
		WHERE MATCH_ID = #{matchId}
	</select>

	<!-- 경기 이벤트 등록 -->
	<insert id="insertMatchEvent" parameterType="MatchEvents">
		INSERT INTO MATCH_EVENTS (
			MATCH_ID,
			TEAM_ID,
			EVENT_TIME,
			EVENT_TYPE,
			PLAYER_ID,
			ASSIST_PLAYER_ID
		) VALUES (
			#{matchId},
			#{teamId},
			#{eventTime},
			#{eventType},
			#{playerId},
			#{assistPlayerId, jdbcType=NUMERIC}
		)
	</insert>

	<!-- 경기별 이벤트 목록 조회 (시간순, 선수/팀 정보 조인) -->
	<select id="selectEventsByMatchId" parameterType="long" resultType="MatchEvents">
		SELECT 
			e.EVENT_ID,
			e.MATCH_ID,
			e.TEAM_ID,
			e.EVENT_TIME,
			e.EVENT_TYPE,
			e.PLAYER_ID,
			e.ASSIST_PLAYER_ID,
			p.NAME AS PLAYER_NAME,
			p.NAME_KOR AS PLAYER_NAME_KOR,
			ap.NAME AS ASSIST_PLAYER_NAME,
			ap.NAME_KOR AS ASSIST_PLAYER_NAME_KOR,
			t.TEAM_NAME,
			t.TEAM_NAME_KOR,
			t.EMBLEM_URL AS TEAM_EMBLEM_URL
		FROM MATCH_EVENTS e
		LEFT JOIN PLAYERS p ON p.PLAYER_ID = e.PLAYER_ID
		LEFT JOIN PLAYERS ap ON ap.PLAYER_ID = e.ASSIST_PLAYER_ID
		LEFT JOIN TEAMS t ON t.TEAM_ID = e.TEAM_ID
		WHERE e.MATCH_ID = #{matchId}
		ORDER BY e.EVENT_TIME ASC, e.EVENT_ID ASC
	</select>

	<!-- 경기별 이벤트 전체 삭제 (재동기화/갱신용) -->
	<delete id="deleteEventsByMatchId" parameterType="long">
		DELETE FROM MATCH_EVENTS
		WHERE MATCH_ID = #{matchId}
	</delete>

</mapper>
