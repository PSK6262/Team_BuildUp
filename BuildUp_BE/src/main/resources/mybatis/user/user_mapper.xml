<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="UserMapper">

	<!-- 신규 회원 가입 -->
	<insert id="insertUser" parameterType="Users" useGeneratedKeys="true" keyProperty="userId" keyColumn="USER_ID">
		INSERT INTO USERS (
			LOGIN_ID,
			PASSWORD,
			NICKNAME,
			EMAIL,
			FAVORITE_TEAM_ID,
			ROLE_CODE,
			POINT,
			CREATED_AT
		) VALUES (
			#{loginId},
			#{password},
			#{nickname},
			#{email},
			#{favoriteTeamId, jdbcType=NUMERIC},
			NVL(#{roleCode, jdbcType=NUMERIC}, 1),
			NVL(#{point, jdbcType=NUMERIC}, 100),
			SYSTIMESTAMP
		)
	</insert>

	<!-- 회원가입 시 승부예측 1:1 통계 초기 레코드 생성 -->
	<insert id="insertUserPredictsInitial" parameterType="long">
		INSERT INTO USER_PREDICTS (
			USER_ID,
			PREDICT_WIN,
			PREDICT_TOTAL,
			UPDATED_AT
		) VALUES (
			#{userId},
			0,
			0,
			SYSTIMESTAMP
		)
	</insert>

	<!-- 로그인 아이디로 회원 단건 조회 (로그인 및 중복검사용) -->
	<select id="selectUserByLoginId" parameterType="string" resultType="Users">
		SELECT 
			u.USER_ID,
			u.LOGIN_ID,
			u.PASSWORD,
			u.NICKNAME,
			u.EMAIL,
			u.FAVORITE_TEAM_ID,
			u.ROLE_CODE,
			u.POINT,
			u.CREATED_AT,
			u.UPDATED_AT,
			r.ROLE_NAME
		FROM USERS u
		LEFT JOIN USER_ROLES r ON u.ROLE_CODE = r.ROLE_CODE
		WHERE u.LOGIN_ID = #{loginId}
	</select>

	<!-- 회원 식별자(PK)로 회원 정보 조회 (마이페이지용) -->
	<select id="selectUserByUserId" parameterType="long" resultType="Users">
		SELECT 
			u.USER_ID,
			u.LOGIN_ID,
			u.PASSWORD,
			u.NICKNAME,
			u.EMAIL,
			u.FAVORITE_TEAM_ID,
			u.ROLE_CODE,
			u.POINT,
			u.CREATED_AT,
			u.UPDATED_AT,
			r.ROLE_NAME
		FROM USERS u
		LEFT JOIN USER_ROLES r ON u.ROLE_CODE = r.ROLE_CODE
		WHERE u.USER_ID = #{userId}
	</select>

	<!-- 로그인 아이디 중복 개수 확인 -->
	<select id="countByLoginId" parameterType="string" resultType="int">
		SELECT COUNT(*)
		FROM USERS
		WHERE LOGIN_ID = #{loginId}
	</select>

	<!-- 닉네임 중복 개수 확인 -->
	<select id="countByNickname" parameterType="string" resultType="int">
		SELECT COUNT(*)
		FROM USERS
		WHERE NICKNAME = #{nickname}
	</select>

	<!-- 이메일 중복 개수 확인 -->
	<select id="countByEmail" parameterType="string" resultType="int">
		SELECT COUNT(*)
		FROM USERS
		WHERE EMAIL = #{email}
	</select>

	<!-- 회원 기본 정보 수정 (닉네임, 이메일, 선호구단) -->
	<update id="updateUser" parameterType="Users">
		UPDATE USERS
		SET 
			NICKNAME = #{nickname},
			EMAIL = #{email},
			FAVORITE_TEAM_ID = #{favoriteTeamId, jdbcType=NUMERIC},
			UPDATED_AT = SYSTIMESTAMP
		WHERE USER_ID = #{userId}
	</update>

	<!-- 비밀번호 변경 -->
	<update id="updatePassword" parameterType="map">
		UPDATE USERS
		SET 
			PASSWORD = #{newPassword},
			UPDATED_AT = SYSTIMESTAMP
		WHERE USER_ID = #{userId}
	</update>

	<!-- 회원 탈퇴: 포인트 변동 내역 삭제 -->
	<delete id="deletePointHistoryByUserId" parameterType="long">
		DELETE FROM POINT_HISTORY
		WHERE USER_ID = #{userId}
	</delete>

	<!-- 회원 탈퇴: 경기 승부예측 내역 삭제 -->
	<delete id="deletePredictionsByUserId" parameterType="long">
		DELETE FROM PREDICTIONS
		WHERE USER_ID = #{userId}
	</delete>

	<!-- 회원 탈퇴: 승부예측 1:1 통계 삭제 -->
	<delete id="deleteUserPredictsByUserId" parameterType="long">
		DELETE FROM USER_PREDICTS
		WHERE USER_ID = #{userId}
	</delete>

	<!-- 회원 탈퇴: 커스텀 팀 AI 매치 내역 삭제 -->
	<delete id="deleteAiMatchesByUserId" parameterType="long">
		DELETE FROM AI_MATCHES
		WHERE HOME_TEAM_ID IN (SELECT CUSTOM_TEAM_ID FROM CUSTOM_TEAMS WHERE USER_ID = #{userId})
		   OR AWAY_TEAM_ID IN (SELECT CUSTOM_TEAM_ID FROM CUSTOM_TEAMS WHERE USER_ID = #{userId})
	</delete>

	<!-- 회원 탈퇴: 커스텀 팀 스쿼드 선수 슬롯 삭제 -->
	<delete id="deleteCustomSquadsByUserId" parameterType="long">
		DELETE FROM CUSTOM_SQUADS
		WHERE CUSTOM_TEAM_ID IN (SELECT CUSTOM_TEAM_ID FROM CUSTOM_TEAMS WHERE USER_ID = #{userId})
	</delete>

	<!-- 회원 탈퇴: 나만의 팀 삭제 -->
	<delete id="deleteCustomTeamsByUserId" parameterType="long">
		DELETE FROM CUSTOM_TEAMS
		WHERE USER_ID = #{userId}
	</delete>

	<!-- 회원 탈퇴: 회원 정보 익명화 및 비활성화 (ROLE_CODE=7 탈퇴회원, 게시글 및 댓글은 보존) -->
	<update id="anonymizeUser" parameterType="long">
		UPDATE USERS
		SET 
			LOGIN_ID = SUBSTR('wd_' || USER_ID || '_' || LOGIN_ID, 1, 50),
			NICKNAME = '(탈퇴회원#' || USER_ID || ')',
			EMAIL = SUBSTR('wd_' || USER_ID || '_' || EMAIL, 1, 100),
			PASSWORD = '*WITHDRAWN*',
			ROLE_CODE = 7,
			POINT = 0,
			FAVORITE_TEAM_ID = NULL,
			UPDATED_AT = SYSTIMESTAMP
		WHERE USER_ID = #{userId}
	</update>

</mapper>
